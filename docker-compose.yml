services:
<<<<<<< HEAD
  # ========================================================================
  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    env_file:
      - .env
    ports:
      - "11434:11434"           # Ollama API port (host 11435 -> container 11434)
    environment:
      # Ensure Ollama can reach MetamCP
      OPENWEB_URL: "http://openwebui:3000"
      METAMCP_URL: "http://app:12008"
      # (optional: any other env vars your model uses)      
      # OLLAMA_NUM_CTX: 100000
      # OLLAMA_CONTEXT_LENGTH: 0 # unlimited context length (be careful with this!)
      OLLAMA_MAX_LOADED_MODELS: 3 # limit to reranker, embedding, and agent/coder
      OLLAMA_NUM_PARALLEL: 3
      # OLLAMA_VULKAN: true # may improve performance 
    volumes:
      - ollama:/root/.ollama     
    gpus: all
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: ${OLLAMA_GPU_COUNT:-1}  # Use all available GPUs by default, or specify with env var
              capabilities: [gpu]
    networks:
      - metamcp-network
    command: ["serve"]
  
  openwebui:
    image: ghcr.io/open-webui/open-webui:main #v0.8.6-cuda #ollama-webui/ollama-webui:main
    container_name: webui
    ports:
      - ${OPEN_WEBUI_PORT_APP:-3000}:8080
    env_file:
      - .env
    environment:
      # Ensure OpenWebUI can reach MetamCP and Ollama
      OLLAMA_API_BASE_URL: "http://ollama:11434/api"
      OLLAMA_BASE_URL: "http://ollama:11434"
      OLLAMA_DOCKER_TAG: ${OLLAMA_DOCKER_TAG_APP:-latest}
      METAMCP_URL: "http://app:12008"
      WEBUI_ADMIN_EMAIL: test@email.com
      WEBUI_ADMIN_PASSWORD: admin
      WEBUI_ADMIN_NAME: admin
      USE_CUDA: ${USE_CUDA_OPENWEBUI:-true}
      USE_OLLAMA: ${USE_OLLAMA_OPENWEBUI:-false}
      # USE_EMBEDDING_MODEL: ${USE_EMBEDDING_MODEL_OPENWEBUI:-sentence-transformers/all-MiniLM-L6-v2}
      USE_RERANKING_MODEL: ${USE_RERANKING_MODEL_OPENWEBUIL:-Qwen/Qwen3-Reranker-0.6B}
      # Database configuration
      PGVECTOR_DB_URL: postgresql://${POSTGRES_USER:-metamcp_user}:${POSTGRES_PASSWORD:-m3t4mcp}@${POSTGRES_HOST:-postgres}:${POSTGRES_PORT:-5432}/${POSTGRES_OPENWEBUI_DB:-openwebui_db}
      DATABASE_URL: postgresql://${POSTGRES_USER:-metamcp_user}:${POSTGRES_PASSWORD:-m3t4mcp}@${POSTGRES_HOST:-postgres}:${POSTGRES_PORT:-5432}/${POSTGRES_OPENWEBUI_DB:-openwebui_db}
      PGVECTOR_CREATE_EXTENSION: false
      HF_TOKEN: ${HF_TOKEN:-}

    depends_on:
      - ollama
      - app
      - postgres
    restart: unless-stopped
    volumes:
      - open_webui:/app/backend/data # Persists data
      - open_webui:/var/lib/.openwebui
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - metamcp-network
    # command: '/bin/bash -c "sleep 5; mongosh $$DATABASE_URL --eval \"show dbs;\""'

=======
>>>>>>> parent of 036626a5 (works well, some small glitches, working on optimizing webui setup)
  app:
    container_name: metamcp
    build:
      dockerfile: Dockerfile.update
      context: .
    pull_policy: always
    env_file:
      - .env
    ports:
      - "12008:12008"
    environment:
      #-- Postgres connection details
      POSTGRES_HOST: ${POSTGRES_HOST:-postgres}
      POSTGRES_PORT: ${POSTGRES_PORT:-5432}
      POSTGRES_USER: ${POSTGRES_USER:-metamcp_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-m3t4mcp}
      POSTGRES_DB: ${POSTGRES_DB:-metamcp_db}
      #-- Database configuration (composed from above vars)
      DATABASE_URL: postgresql://${POSTGRES_USER:-metamcp_user}:${POSTGRES_PASSWORD:-m3t4mcp}@${POSTGRES_HOST:-postgres}:${POSTGRES_PORT:-5432}/${POSTGRES_DB:-metamcp_db}
      #-- Application URL configuration
      APP_URL: ${APP_URL:-http://localhost:12008}
      NEXT_PUBLIC_APP_URL: ${APP_URL:-http://localhost:12008}      
      #-- Auth configuration
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET:-your-super-secret-key-change-this-in-production}      
      #-- Docker networking fix
      TRANSFORM_LOCALHOST_TO_DOCKER_INTERNAL: ${TRANSFORM_LOCALHOST_TO_DOCKER_INTERNAL:-true}
      
      # ========================================================================
      # Bootstrap Configuration Examples
      # ========================================================================
      
      # Multiple Users - Define different users with different roles
      # BOOTSTRAP_USERS: |
      #   [
      #     {
      #       "email": "admin@company.com",
      #       "password": "admin-secure-password",
      #       "name": "System Administrator"
      #     },
      #     {
      #       "email": "developer@company.com",
      #       "password": "dev-secure-password",
      #       "name": "Lead Developer"
      #     },
      #     {
      #       "email": "analyst@company.com",
      #       "password": "analyst-secure-password",
      #       "name": "Data Analyst"
      #     }
      #   ]
      
      # API Keys - Assign keys to specific users using user_email
      # BOOTSTRAP_API_KEYS: |
      #   [
      #     {
      #       "name": "Admin Production Key",
      #       "is_public": false,
      #       "user_email": "admin@company.com"
      #     },
      #     {
      #       "name": "Developer Key",
      #       "is_public": false,
      #       "user_email": "developer@company.com"
      #     },
      #     {
      #       "name": "Analyst Key",
      #       "is_public": false,
      #       "user_email": "analyst@company.com"
      #     },
      #     {
      #       "name": "Public API Key",
      #       "is_public": true
      #     }
      #   ]
      
      # Namespaces - Create user-specific workspaces
      # BOOTSTRAP_NAMESPACES: |
      #   [
      #     {
      #       "name": "Admin Workspace",
      #       "description": "Administrator's private workspace",
      #       "is_public": false,
      #       "user_email": "admin@company.com",
      #       "update": true
      #     },
      #     {
      #       "name": "Development",
      #       "description": "Development environment",
      #       "is_public": false,
      #       "user_email": "developer@company.com",
      #       "update": true
      #     },
      #     {
      #       "name": "Analytics",
      #       "description": "Data analytics workspace",
      #       "is_public": false,
      #       "user_email": "analyst@company.com",
      #       "update": true
      #     },
      #     {
      #       "name": "Shared Resources",
      #       "description": "Public shared namespace",
      #       "is_public": true,
      #       "update": true
      #     }
      #   ]
      
      # Endpoints - Configure endpoints with different owners and auth settings
      # Note: Endpoints can optionally specify which namespace they should be created in
      # BOOTSTRAP_ENDPOINTS: |
      #   [
      #     {
      #       "name": "admin-api",
      #       "description": "Admin-only API endpoint",
      #       "namespace": "Production",
      #       "enable_auth": true,
      #       "enable_auth_query": false,
      #       "enable_auth_oauth": true,
      #       "is_public": false,
      #       "user_email": "admin@company.com",
      #       "update": true
      #     },
      #     {
      #       "name": "dev-api",
      #       "description": "Development API endpoint",
      #       "namespace": "Development",
      #       "enable_auth": true,
      #       "enable_auth_query": true,
      #       "enable_auth_oauth": false,
      #       "is_public": false,
      #       "user_email": "developer@company.com",
      #       "update": true
      #     },
      #     {
      #       "name": "analytics-api",
      #       "description": "Analytics API endpoint",
      #       "namespace": "Analytics",
      #       "enable_auth": true,
      #       "enable_auth_query": false,
      #       "enable_auth_oauth": false,
      #       "is_public": false,
      #       "user_email": "analyst@company.com",
      #       "update": true
      #     },
      #     {
      #       "name": "public-api",
      #       "description": "Public API endpoint - no auth required",
      #       "namespace": "Shared Resources",
      #       "enable_auth": false,
      #       "enable_auth_query": false,
      #       "enable_auth_oauth": false,
      #       "is_public": true,
      #       "update": true
      #     }
      #   ]
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"
    networks:
      - metamcp-network

  postgres:
    image: pgvector/pgvector:pg16 #postgres:16-alpine
    container_name: metamcp-pg
    restart: unless-stopped
    env_file:
      - .env
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-metamcp_db}
      POSTGRES_USER: ${POSTGRES_USER:-metamcp_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-m3t4mcp}
    ports:
      - "${POSTGRES_EXTERNAL_PORT:-9433}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-metamcp_user} -d ${POSTGRES_DB:-metamcp_db}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - metamcp-network

  # pgvector:
  #   image: pgvector/pgvector:pg18-trixie
  #   container_name: pgvector
  #   restart: unless-stopped
  #   env_file:
  #     - .env
  #   environment:
  #     POSTGRES_DB: ${PGVECTOR_DB:-openwebui_db_vec}
  #     POSTGRES_USER: ${PGVECTOR_USER:-openwebui_user_vec}
  #     POSTGRES_PASSWORD: ${PGVECTOR_PASSWORD:-0p3nw3b_vec}
  #   ports:
  #     - "${PGVECTOR_EXTERNAL_PORT:-9434}:5432"
  #   volumes:
  #     # - ./pg-init-scripts:/docker-entrypoint-initdb.d
  #     - pgvector_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U ${PGVECTOR_USER:-openwebui_db_vec} -d ${PGVECTOR_DB:-openwebui_db_vec}"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #     start_period: 30s
  #   networks:
  #     - metamcp-network

volumes:
<<<<<<< HEAD
  ollama:
  open_webui:
=======
>>>>>>> parent of 036626a5 (works well, some small glitches, working on optimizing webui setup)
  postgres_data:
    driver: local
  pgvector_data:
    driver: local

networks:
  metamcp-network:
    driver: bridge
